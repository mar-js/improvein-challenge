import styled from 'styled-components'

export const HeaderContainer = styled.header`
  width: 100%;
  height: auto;
  margin: 0 0 2rem;
  padding: 0 1rem;
  box-shadow: 0 .1rem .5rem var(--color-black);
`
